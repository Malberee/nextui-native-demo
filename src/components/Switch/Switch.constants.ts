import { Sizes } from '../../types'

export const switchSizes: Pick<Sizes, 'sm' | 'md' | 'lg'> = {
  sm: 40,
  md: 48,
  lg: 56,
}

export const thumbSizes: Pick<Sizes, 'sm' | 'md' | 'lg'> = {
  sm: 16,
  md: 20,
  lg: 24,
}
