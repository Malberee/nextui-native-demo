import { Sizes } from '../../types'

export const sizes: Pick<Sizes, 'sm' | 'md' | 'lg'> = {
  sm: 16,
  md: 20,
  lg: 24,
}

export const labelFontSizes: Pick<Sizes, 'sm' | 'md' | 'lg'> = {
  sm: 14,
  md: 16,
  lg: 18,
}

export const descriptionFontSizes: Pick<Sizes, 'sm' | 'md' | 'lg'> = {
  sm: 12,
  md: 14,
  lg: 16,
}
